services:
  devstretch:
    build: .
    container_name: devstretch-plus
    restart: always          # auto-start on boot / restart on crash
    ports:
      - "7300:80"            # 1337 reversed — http://localhost:7300
